cxf:
  path: /soap-api
spring:
  datasource:
    url: jdbc:sqlite:cinema.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: true

